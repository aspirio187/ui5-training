<mvc:View
    controllerName="sap.ui.demo.walkthrough.controller.App"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc">
        <Button 
            text="{i18n>showHelloButtonText}"
            press=".onShowHello" />

        <Input 
            value="{recip>/recipient/name}"
            description="Hello {recip>/recipient/name}"
            valueLiveUpdate="true"
            width="60%" />
</mvc:View>